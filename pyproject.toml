[build-system]
requires = [
    "setuptools",
    "pandas",
    "networkx",
    "matplotlib",
    "wheel",
    "numpy",
    "pandas",
    "plotnine",
    "scipy"
    ]
build-backend = "setuptools.build_meta"

