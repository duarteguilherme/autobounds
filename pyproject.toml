[build-system]
requires = [
    "setuptools",
    "pandas",
    "networkx",
    "matplotlib",
    "wheel",
    "numpy",
    "pandas",
    "plotnine",
    "sympy",
    "scipy"
    ]
build-backend = "setuptools.build_meta"

